import { apiConversationFeature } from './api-conversation-feature';

describe('apiConversationFeature', () => {
  it('should work', () => {
    expect(apiConversationFeature()).toEqual('api-conversation-feature');
  });
});
