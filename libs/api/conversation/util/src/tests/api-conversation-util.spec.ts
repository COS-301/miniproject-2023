import { apiConversationUtil } from './api-conversation-util';

describe('apiConversationUtil', () => {
  it('should work', () => {
    expect(apiConversationUtil()).toEqual('api-conversation-util');
  });
});
