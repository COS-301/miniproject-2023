import { apiApiTimeUpdateUtil } from './api-api-time-update-util';

describe('apiApiTimeUpdateUtil', () => {
  it('should work', () => {
    expect(apiApiTimeUpdateUtil()).toEqual('api-api-time-update-util');
  });
});
