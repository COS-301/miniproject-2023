import { apiApiTimeUpdateFeature } from './api-api-time-update-feature';

describe('apiApiTimeUpdateFeature', () => {
  it('should work', () => {
    expect(apiApiTimeUpdateFeature()).toEqual('api-api-time-update-feature');
  });
});
