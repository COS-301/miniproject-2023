import { apiMessagesFeature } from './api-messages-feature';

describe('apiMessagesFeature', () => {
  it('should work', () => {
    expect(apiMessagesFeature()).toEqual('api-messages-feature');
  });
});
