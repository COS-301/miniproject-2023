import { apiMessagesDataAccess } from './api-messages-data-access';

describe('apiMessagesDataAccess', () => {
  it('should work', () => {
    expect(apiMessagesDataAccess()).toEqual('api-messages-data-access');
  });
});
