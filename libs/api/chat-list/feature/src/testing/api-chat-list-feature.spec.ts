import { apiChatListFeature } from './api-chat-list-feature';

describe('apiChatListFeature', () => {
  it('should work', () => {
    expect(apiChatListFeature()).toEqual('api-chat-list-feature');
  });
});
