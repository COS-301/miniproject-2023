import { apiFriendDataAccess } from './api-friend-data-access';

describe('apiFriendDataAccess', () => {
  it('should work', () => {
    expect(apiFriendDataAccess()).toEqual('api-friend-data-access');
  });
});
