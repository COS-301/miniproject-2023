import { apiFriendRequestFeature } from './api-friend-request-feature';

describe('apiFriendRequestFeature', () => {
  it('should work', () => {
    expect(apiFriendRequestFeature()).toEqual('api-friend-request-feature');
  });
});
