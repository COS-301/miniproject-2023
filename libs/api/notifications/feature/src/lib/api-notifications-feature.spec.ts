import { apiNotificationsFeature } from './api-notifications-feature';

describe('apiNotificationsFeature', () => {
  it('should work', () => {
    expect(apiNotificationsFeature()).toEqual('api-notifications-feature');
  });
});
