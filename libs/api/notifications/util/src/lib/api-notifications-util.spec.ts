import { apiNotificationsUtil } from './api-notifications-util';

describe('apiNotificationsUtil', () => {
  it('should work', () => {
    expect(apiNotificationsUtil()).toEqual('api-notifications-util');
  });
});
