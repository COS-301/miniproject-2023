import { appNotificationsUtil } from './app-notifications-util';

describe('appNotificationsUtil', () => {
  it('should work', () => {
    expect(appNotificationsUtil()).toEqual('app-notifications-util');
  });
});
