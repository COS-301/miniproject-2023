import { appNotificationsDataAccess } from './app-notifications-data-access';

describe('appNotificationsDataAccess', () => {
  it('should work', () => {
    expect(appNotificationsDataAccess()).toEqual(
      'app-notifications-data-access'
    );
  });
});
