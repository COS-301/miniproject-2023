import { appSearchDataAccess } from './app-search-data-access';

describe('appSearchDataAccess', () => {
  it('should work', () => {
    expect(appSearchDataAccess()).toEqual('app-search-data-access');
  });
});
