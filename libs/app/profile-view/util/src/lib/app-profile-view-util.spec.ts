import { appProfileViewUtil } from './app-profile-view-util';

describe('appProfileViewUtil', () => {
  it('should work', () => {
    expect(appProfileViewUtil()).toEqual('app-profile-view-util');
  });
});
