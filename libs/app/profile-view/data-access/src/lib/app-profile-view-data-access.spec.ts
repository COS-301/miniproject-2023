import { appProfileViewDataAccess } from './app-profile-view-data-access';

describe('appProfileViewDataAccess', () => {
  it('should work', () => {
    expect(appProfileViewDataAccess()).toEqual('app-profile-view-data-access');
  });
});
