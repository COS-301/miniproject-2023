import { appOtherUserDataAccess } from './app-other-user-data-access';

describe('appOtherUserDataAccess', () => {
  it('should work', () => {
    expect(appOtherUserDataAccess()).toEqual('app-other-user-data-access');
  });
});
