import { appOtherUserFeature } from './app-other-user-feature';

describe('appOtherUserFeature', () => {
  it('should work', () => {
    expect(appOtherUserFeature()).toEqual('app-other-user-feature');
  });
});
