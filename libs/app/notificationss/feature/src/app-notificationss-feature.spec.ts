import { appNotificationssFeature } from './app-notificationss-feature';

describe('appNotificationssFeature', () => {
  it('should work', () => {
    expect(appNotificationssFeature()).toEqual('app-notificationss-feature');
  });
});
