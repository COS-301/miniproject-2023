import { appChallengeDataAccess } from './app-challenge-data-access';

describe('appChallengeDataAccess', () => {
  it('should work', () => {
    expect(appChallengeDataAccess()).toEqual('app-challenge-data-access');
  });
});
