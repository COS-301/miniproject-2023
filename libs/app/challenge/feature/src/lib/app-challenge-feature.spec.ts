import { appChallengeFeature } from './app-challenge-feature';

describe('appChallengeFeature', () => {
  it('should work', () => {
    expect(appChallengeFeature()).toEqual('app-challenge-feature');
  });
});
