import { appUserDataAccess } from './app-user-data-access';

describe('appUserDataAccess', () => {
  it('should work', () => {
    expect(appUserDataAccess()).toEqual('app-user-data-access');
  });
});
